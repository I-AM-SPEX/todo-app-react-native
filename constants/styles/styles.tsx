import { StyleSheet } from "react-native";

const globalStyles = StyleSheet.create({
  page: {
    borderWidth: 1,
    borderColor: "black",
    flex: 1,
    padding: 8,
    alignItems: "center",
  },
});

export { globalStyles };
